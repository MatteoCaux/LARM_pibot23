launch:

- node:
    pkg: "rviz2"
    exec: "rviz2"
    name: "rviz2"
    args: "-d $(find-pkg-share tutorial_pkg)/rviz/rviz_tbot.rviz"

- node:
    pkg: "tuto_vision"
    exec: "vision_robot"
    name: "vision_robot"
    
#- executable:
    #cmd: gnome-terminal --tab -e 'ros2 run teleop_twist_keyboard teleop_twist_keyboard' # cmd_vel:=/multi/cmd_teleop
    # cmd: gnome-terminal --tab -e 'ros2 run tuto_vision vision_robot'
